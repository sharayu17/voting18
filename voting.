#include<iostream>
using namespace std;

int main() {

    int age;
    cout << "Enter age of a user:";
    cin>>age;

    if (age >= 18) {
        cout << "Yes!! You can Vote";
    } else {
        cout << "No!! You are not eligible for Voting";
    }

    return 0;
}
